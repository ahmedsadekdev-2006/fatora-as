# تقرير تدقيق التسليم

تم اختبار النسخة المتصلة بقاعدة MongoDB الفعلية بدل الاعتماد على بيانات عشوائية محلية. صفحة الدخول تظهر عند غياب الجلسة، وتم التحقق من الدخول بحساب `admin`، ثم ظهرت لوحة التحكم وقائمة المستخدمين للمشرف فقط. كما تم التحقق من الخروج وإعادة ظهور صفحة الدخول.

| المسار | نتيجة التحقق |
|---|---|
| تسجيل الدخول ADMIN | ناجح من الواجهة وAPI |
| تسجيل الدخول USER ومنع بيانات التكلفة | ناجح عبر API، والتكلفة لا تُعاد للمستخدم العادي |
| لوحة التحكم | ناجحة، وتعرض العملاء والفواتير والمصاريف من MongoDB |
| زر عميل سريع | يفتح نموذج الإضافة |
| زر صنف جديد | يفتح نموذج الإضافة للمشرف |
| زر مصروف سريع من لوحة التحكم | يفتح نموذج المصروف ويظهر التصنيفات العربية |
| العملاء | عرض وبحث وأزرار تعديل وتعطيل ظاهرة، والمسارات موصولة |
| الأصناف | عرض المخزون وأسعار البيع والتكلفة للمشرف وأزرار التعديل والتعطيل |
| الفواتير | عرض الفواتير القديمة، اسم المنشئ، وفتح التفاصيل |
| إنشاء فاتورة | مسار API يعمل مع عميل موجود أو عميل جديد بالاسم، مع اسم المنشئ وتحديث الحساب والمخزون |
| إلغاء فاتورة | موصول كإلغاء يحافظ على السجل بدل الحذف المالي |
| المصاريف | عرض وإضافة وتعديل وحذف موصولة، وتم اختبار الإضافة من الواجهة ثم إزالة بيانات الاختبار |
| المستخدمون | عرض للمشرف فقط، تعديل الدور/الاسم، وتعطيل الحساب |
| التقارير | تعرض المؤشرات المتاحة حاليًا للمشرف، بينما التصدير والفلاتر المتقدمة ما زالت ضمن التوسعة |

تم تنظيف مصروف الاختبار بعد التحقق. الحالة النهائية في MongoDB: مستخدمان تشغيليان، ثلاثة عملاء، أربعة أصناف، فاتورتان، دفعة واحدة، وثلاثة مصاريف عربية مترابطة. نجحت أوامر `pnpm check` و`pnpm test` و`pnpm build`، ونجحت اختبارات الاتصال بالمونجو والصلاحيات والمعادلات المالية.

## جولة واجهة إضافية

تم تسجيل الدخول من صفحة login وفتح الأقسام الرئيسية من القائمة. ظهر زر المصروف السريع داخل لوحة التحكم وفتح نموذج المصروف مع التصنيفات العربية. تم تنفيذ تعديل عميل من الواجهة وإرجاع الاسم الأصلي، وتنفيذ تعديل صنف وإرجاع السعر/الاسم الأصلي، وتنفيذ تعديل مستخدم وإرجاع الاسم الأصلي. كما تم فتح نموذج الفاتورة والتأكد من ظهور العميل والصنف والكمية والسعر والخصم والمدفوع ورسالة الحفظ؛ وتم تعديل الزر ليكون `type=submit` ويعرض سبب الرفض بدل التعطيل الصامت.

لا توجد بيانات اختبار مؤقتة متروكة في MongoDB بعد الجولة؛ تم حذف مصروف الاختبار والتأكد من عودة العدد إلى ثلاثة مصاريف تشغيلية. لا تزال شاشة الفاتورة الحالية أحادية السطر، وتظل الفلاتر والتصدير المتقدم وتاريخ حساب العميل من التوسعات غير المنفذة.

## جولة الحساب والتقارير والمزامنة

| المسار | نتيجة التحقق |
|---|---|
| الحقول الفارغة في login | ناجح بعد إعادة تحميل كاملة؛ لا يظهر اسم المستخدم أو كلمة المرور حتى يبدأ المستخدم التفاعل |
| كشف حساب العميل | ناجح من صفحة العملاء؛ يعرض الرصيد الحالي، إجمالي الفواتير، المدفوع، وقائمة الفواتير المرتبطة مع اسم المنشئ والتاريخ |
| تقارير التاريخ | ناجح؛ حقلا من/إلى ظاهرين ويؤثران على مؤشرات الصفحة الرئيسية والتقارير |
| تصدير CSV | ناجح؛ زر التصدير ظاهر في التقارير وينشئ ملف CSV بترميز UTF-8 مع بيانات الفاتورة والتكلفة للمشرف |
| مزامنة offline | أضيف مسار `/api/sync` بمعالجة idempotency للعمليات المؤجلة، مع منع طابور العمليات التي حُفظت مباشرة أثناء الاتصال |

تمت مراجعة البناء بعد التعديلات ونجح `pnpm check` و`pnpm build`. اختبار MongoDB المنفصل تعثر في هذه الجولة بسبب إغلاق اتصال الشبكة أثناء مهلة الاختبار، وليس بسبب خطأ TypeScript أو بناء.

| كشف حساب العميل بعد ربط المدفوعات | ناجح؛ ظهر سطر دفعة نقدية مع التاريخ، المدين/الدائن، إلى جانب الفاتورة المرتبطة |
| تصدير CSV من صفحة الفواتير | ناجح؛ ظهر زر التصدير بجوار فاتورة جديدة، ويستخدم أعمدة التكلفة للمشرف فقط |

## الاختبارات الآلية الأخيرة

تم تشغيل `pnpm test` بنجاح: ثلاثة ملفات اختبار، سبع حالات اختبار ناجحة، بما فيها اختبار اتصال MongoDB بعد ضبط مهلة الاختبار، اختبارات المصادقة، والمعادلات المالية متعددة الأصناف.

| اختبار الصلاحيات وتنقية التكلفة | ناجح؛ اختبر `sanitizeProductForRole` و`sanitizeInvoiceForRole` لمنع تكلفة الشراء عن USER والإبقاء عليها لـ ADMIN |
| اختبار منطق الفاتورة متعددة الأصناف | ناجح؛ الاختبار يستدعي وحدة الخادم المشتركة `invoiceLogic.ts` ويتحقق من subtotal والتكلفة والإجمالي والمتبقي |

## جدول تدقيق الأزرار المرئية

| القسم | الزر/التدفق | النتيجة الحالية |
|---|---|---|
| الدخول | دخول، خروج | ناجح؛ يفتح الجلسة وينهيها دون autofill ابتدائي |
| التنقل | الرئيسية، الفواتير، العملاء، الأصناف، المصاريف، التقارير، المستخدمون | ناجح؛ كل زر يغير القسم، والمستخدمون للمشرف فقط |
| الرئيسية | عميل، صنف جديد، مصروف | ناجح؛ تفتح الحوارات السريعة المناسبة |
| العملاء | إضافة عميل، تعديل، حذف، كشف الحساب | موصول؛ كشف الحساب اختُبر مع دفعة وفاتورة، وتحتاج عمليات الحذف/التعديل جولة منفصلة |
| الأصناف | إضافة صنف، تعديل، حذف | موصول؛ حوار التعديل اختُبر، وتحتاج عملية الحذف/التعطيل جولة منفصلة |
| الفواتير | فاتورة جديدة، فتح التفاصيل، تصدير CSV | موصول؛ الإنشاء متعدد الأصناف والتصدير ظاهران، وتعديل/إلغاء الفاتورة يحتاجان جولة منفصلة |
| المصاريف | إضافة، تعديل، حذف | موصول؛ الإضافة السريعة اختُبرت، وتحتاج عمليتا التعديل والحذف جولة منفصلة |
| التقارير | من تاريخ، إلى تاريخ، كل الفترات، تصدير CSV | موصول؛ الفلاتر والتصدير ظاهران وتم اختبارهما بصريًا |
| المستخدمون | تعديل، تعطيل | موصول للمشرف، ويحتاج تدفق تأكيد كامل في الجولة النهائية |
| الحوارات | حفظ، إغلاق، تأكيد، إلغاء | حوارات داخلية بخلفية بيضاء؛ حوار تعديل الصنف والتأكيد اختُبرا بصريًا |

| تعارضات المزامنة | أضيف رفض خادمي للتحديث المحلي الأقدم وفق `updatedAt` مع HTTP 409، وتحويل العملية إلى `CONFLICT` في Dexie بدل إعادة المحاولة العمياء |
| الاختبارات بعد سياسة التعارض | ناجحة؛ 10 اختبارات مستهدفة، ونجح `pnpm check` و`pnpm build` |

| تفاصيل الفاتورة | ناجح؛ ظهر اسم العميل، اسم الحساب المنشئ، السطر، الإجمالي، والمتبقي |
| حوار تعديل الفاتورة | ناجح بصريًا؛ ظهرت حقول العميل والكمية وسعر البيع والخصم والمدفوع، وأُغلق دون حفظ تغيير |
| حوار إلغاء الفاتورة | ناجح بصريًا؛ ظهر تحذير الاحتفاظ بالسجل المالي مع زري رجوع/تأكيد، وتم اختيار الرجوع للحفاظ على بيانات التشغيل |

## سجل الإجراءات الفردية

| الإجراء الفردي | نتيجة | الدليل/الملاحظة |
|---|---|---|
| دخول | ناجح | جلسة ADMIN فتحت لوحة العمل |
| خروج | ناجح | موثق في الجولة السابقة وأعاد login |
| الرئيسية | ناجح | يعرض المؤشرات والعمليات الأخيرة |
| الفواتير | ناجح | يفتح قائمة الفواتير |
| العملاء | ناجح | يفتح قائمة العملاء والبحث |
| الأصناف | ناجح | يفتح قائمة المخزون |
| المصاريف | ناجح | يفتح قائمة المصاريف |
| التقارير | ناجح | يفتح المؤشرات والفلاتر والتصدير |
| المستخدمون | ناجح للمشرف | يظهر تبويب الإدارة للمشرف فقط |
| إضافة عميل السريع | موصول | يفتح الحوار؛ لا توجد عملية اختبار جديدة في هذه الجولة |
| إضافة صنف السريع | موصول للمشرف | يفتح الحوار؛ لا توجد عملية اختبار جديدة في هذه الجولة |
| إضافة مصروف السريع | ناجح | فُتح واختُبر سابقًا مع تنظيف البيانات |
| بحث العملاء | ناجح | ضيّق النتائج بالاسم داخل الفاتورة |
| بحث الأصناف | ناجح | ضيّق النتائج بالاسم/SKU داخل الفاتورة |
| فاتورة جديدة | ناجح | يفتح نموذج متعدد الأسطر وحقولًا فارغة |
| حفظ فاتورة | ناجح سابقًا | تحقق API والواجهة مع عميل موجود/جديد وتحديث الحساب والمخزون |
| فتح تفاصيل فاتورة | ناجح | ظهر العميل والمنشئ والسطر والإجمالي والمتبقي |
| تصدير فواتير CSV | ظاهر وموصول | الزر ظهر في صفحة الفواتير؛ تنزيل الملف لم يُفحص من chrome://downloads |
| تعديل الفاتورة | حوار ناجح بصريًا | ظهرت الحقول؛ أُغلق دون حفظ تغيير |
| إلغاء الفاتورة | تأكيد ناجح بصريًا | ظهر التحذير؛ تم اختيار رجوع للحفاظ على البيانات |
| إضافة/تعديل/حذف عميل | API موصول | التعديل والحذف يحتاجان اختبارًا على نسخة بيانات منفصلة |
| كشف الحساب | ناجح | ظهر الرصيد والفاتورة والدفعة وحركة المدين/الدائن |
| إضافة/تعديل/حذف صنف | API موصول | التعديل اختُبر بصريًا؛ الحذف يحتاج نسخة منفصلة |
| إضافة/تعديل/حذف مصروف | API موصول | الإضافة اختُبرت؛ التعديل والحذف يحتاجان نسخة منفصلة |
| تعديل/تعطيل مستخدم | API موصول للمشرف | يحتاج تدفقًا منفصلًا على مستخدم مؤقت |
| من تاريخ/إلى تاريخ | ناجح بصريًا | حقلا التاريخ ظاهران في الرئيسية والتقارير |
| كل الفترات | ناجح بصريًا | زر إعادة النطاق ظاهر |
| تصدير تقرير CSV | ظاهر وموصول | الزر ظهر في التقارير؛ صلاحية التكلفة خادمية للمشرف |
| حفظ الحوار | موصول | حوارات CRUD تستدعي إجراءات الحفظ الحالية |
| إغلاق الحوار | ناجح | أُغلقت حوارات الفاتورة والحساب دون حفظ |
| تأكيد/رجوع الإلغاء | ناجح بصريًا | ظهر الزران وتم اختبار الرجوع |

## Regression pass

آخر تشغيل كامل نجح: `pnpm test` عبر خمسة ملفات واثنتي عشرة حالة، ثم `pnpm check` و`pnpm build`. اختبار اتصال MongoDB نجح بمهلة 15 ثانية، مع بقاء تحذير حجم حزمة Vite غير مانع للبناء.

| API تسجيل دفعة وتوزيعها | أضيف على الخادم؛ يوزع الدفعة على أقدم الفواتير، يحدّث paidAmount/remainingAmount/status ورصيد العميل، ويحمي العملية بـ clientOperationId. تم اختبار دالة التوزيع 11/11، ولم تُنفذ واجهة تسجيل دفعة مستقلة بعد. |

| واجهة تسجيل الدفعة | ناجح بصريًا؛ كشف حساب أحمد محمد أظهر حقل المبلغ، قائمة نقدي/تحويل بنكي/أخرى، وزر حفظ الدفعة، مع إبقاء التنفيذ غير المرسل في الجولة حفاظًا على بيانات التسليم |

## Payment UI regression

بعد إضافة واجهة تسجيل الدفعة ومسار التوزيع: `pnpm test` نجح عبر 5 ملفات و13 حالة، ونجح `pnpm check` و`pnpm build`. اختبار MongoDB ما زال ناجحًا ضمن المجموعة، وتحذير حجم bundle غير مانع.

| CRUD عميل مؤقت | ناجح؛ أُنشئ `عميل تدقيق مؤقت` عبر POST `/api/customers` بحالة 201، ثم حُذف عبر DELETE بحالة 200، وتحقق GET نهائي من count=3 وعدم وجود الاسم المؤقت |

| مزامنة payment وstockMovement | أضيف دعم خادمي للعمليتين، مع تحقق من customerId/productId وصلاحية القيم وحفظ idempotency؛ اختبار عقد الأنواع/backoff/conflict نجح 8/8، بينما اختبار HTTP حي لمسار `/api/sync` ما زال مطلوبًا |

| عقد أنواع المزامنة | ناجح؛ `payment` و`stockMovement` أصبحا مدعومين في الخادم، واختبار الأنواع/backoff/conflict نجح 8/8، مع بقاء اختبار HTTP حي لمسار `/api/sync` مطلوبًا قبل الإغلاق النهائي |

| اختبار HTTP `/api/sync` | ناجح في جلسة ADMIN على عميل موجود دون تغيير حقوله التجارية: العملية الأولى أعادت 200، إعادة نفس `clientOperationId` أعادت `duplicate:true`، وتحديث قديم منفصل أعاد 409 مع رسالة تعارض. الكود المحلي يحول 409 إلى CONFLICT في Dexie. |

| إنشاء/حذف عميل من الواجهة | ناجح؛ أُنشئ `عميل تعديل تدقيق` من حوار الإضافة بحالة 201، وسُجلت مزامنته في `/api/sync` بحالة 200، ثم حُذف عبر DELETE بحالة 200. لم تُحفظ أي بيانات تجارية دائمة من الجولة. فتح حوار التعديل موثق، أما حفظ تعديل مستقل فيحتاج جولة لاحقة. |

| منطق إلغاء الفاتورة | اختبار خادمي ناجح؛ كل سطر يعاد إلى المخزون، تُنشأ حركة `RETURN`، يُخفض رصيد العميل بالمتبقي، ويصبح المتبقي صفرًا والحالة `CANCELLED`. الإرجاع التشغيلي في هذه النسخة هو آلية الإلغاء العكسي وليس endpoint مستقلًا. |
| اختبار الإلغاء الحي | مؤجل عمدًا على بيانات التشغيل؛ يلزم سجل فاتورة مؤقت قابل للاستعادة لأن الإلغاء ينشئ حركة مالية/مخزنية دائمة. |

| Regression بعد الإلغاء العكسي | ناجح؛ 5 ملفات اختبار و16 اختبارًا، مع نجاح اتصال MongoDB، TypeScript، والبناء. اختبار `calculateCancellationEffect` يثبت استعادة السطور وخفض رصيد العميل وتصفير المتبقي، بينما اختبار إلغاء API حيًا على فاتورة منفصلة ما زال مؤجلًا حفاظًا على السجل المالي. |

| Login autofill | ناجح بصريًا بعد إعادة تحميل الصفحة؛ حقلا اسم المستخدم وكلمة المرور ظهرا فارغين مع placeholder، رغم وجود بيانات محفوظة سابقًا في جلسة المتصفح. أضيفت حقول فخ غير تفاعلية وأسماء حقول off للحماية من Chrome autofill. |

| MongoDB consistency | تم تثبيت اسم القاعدة المشتق من `MONGODB_DB` أو مسار URI مع fallback إلى `fatora`، وتسجيل اسم القاعدة غير السري عند الاتصال. اختبار الاتصال فشل مرة بسبب انقطاع شبكة MongoDB ثم نجح منفردًا بعد عودة الاتصال، كما نجح TypeScript والبناء. |

| Dexie sync integration | ناجح؛ اختبار IndexedDB افتراضية مرّر عملية إلى SYNCED، ثم أثبت تحويل 503 إلى FAILED و409 إلى CONFLICT مع بقاء retryCount والحالة في الطابور. إجمالي regression أصبح 6 ملفات و18 اختبارًا، مع نجاح TypeScript والبناء. |

| Service worker وبيانات API | اكتُشف أن sw.js كان يخزن GET لمسارات `/api/` مما أبقى سجلات QA المحذوفة في واجهة المتصفح. تم إصدار cache v3 واستثناء `/api/` مع `cache: no-store` وعدم fallback إلى بيانات API قديمة. بعد الإصلاح نجحت 6 ملفات و18 اختبارًا وTypeScript والبناء. |

| تنظيف بيانات QA بعد اختبارات CRUD والإلغاء | ناجح؛ أُلغيت الفواتير الاختبارية الثلاث عبر المسار الرسمي، عاد رصيد مؤسسة النور إلى 0، اختفت السجلات المؤقتة من القراءة الشبكية غير المخزنة، وأُعيد اسم صنف السكر إلى `سكر أبيض 1 كجم` مع مخزون 90. النتيجة النهائية: 3 عملاء تشغيلية، 0 صنف باسم مراجعة، والفواتير الاختبارية موجودة كـCANCELLED حفاظًا على الأثر المالي. |


## 2026-08-20 — مراجعة تعديل الفاتورة متعدد الأصناف والحوارات الطويلة

تم فتح فاتورة تحتوي على صنفين من صفحة الفواتير، ثم فتح التفاصيل وحوار التعديل. ظهر الصنفان كسطرين مستقلين، ولكل سطر حقلا كمية وسعر بيع منفصلان، مع إجمالي السطر واسم الصنف. تم التحقق بصريًا من ظهور حقلي الخصم والمدفوع، ومن قابلية الحوار للتمرير الرأسي على viewport قصير.

على مستوى الخادم، أصبح `PUT /api/invoices/:id` يستقبل `items[]`، ويعيد حساب subtotal وtotal وremaining، ويحسب فروق الكميات لكل منتج، ويحدث المخزون بحركات `INVOICE_EDIT`، ويعدل رصيد العميل بالفارق في المتبقي. أضيف اختبار مستقل لدالة فروق الكميات للمنتجات المضافة والمخفضة والمحذوفة.

تم تعميم `max-height` و`overflow-y-auto` و`overscroll-contain` على Dialog الأساسي والحوارات السريعة، بحيث لا يخرج المحتوى الطويل من الشاشة وتظل أزرار الإجراء قابلة للوصول بعد التمرير.

نتيجة التحقق: `pnpm test` نجح بعدد 19 اختبارًا، و`pnpm check` و`pnpm build` نجحا. لم تُنشأ بيانات تشغيلية جديدة أثناء هذه الجولة.


## 2026-08-20 — اختبار تكاملي حي لمسار الفاتورة بعد إصلاح التكرار

شُغّل الاختبار `server/invoice.edit.integration.test.ts` على خادم التطوير المتصل بقاعدة MongoDB. أنشأ الاختبار عميلًا وصنفين مؤقتين وفاتورة من عميل موجود، ثم عدّل السطرين معًا، وحقق أن الإجمالي أصبح 30 والمتبقي 25، وأن المخزون أصبح 8 و9، ورصيد العميل 25، وسُجلت حركة `INVOICE_EDIT`. بعد ذلك ألغى الفاتورة وتحقق من عودة المخزون إلى 10 و10 ورصيد العميل إلى صفر.

كما أنشأ الاختبار فاتورة ثانية بعميل جديد بالاسم فقط، وتحقق من إنشاء العميل ورصيده، ثم ألغى الفاتورة وتحقق من تصفير الرصيد. في النهاية حذف الاختبار الفواتير والعملاء والأصناف وحركات المخزون المؤقتة بالكامل. النتيجة: اختبار تكاملي واحد ناجح ومدته نحو 29 ثانية، دون بقاء بيانات اختبار.

يبقى الإثبات المرئي الكامل لعمليات الحفظ/التعديل/التعطيل/الإلغاء والتصدير الفعلي منفصلًا عن اختبار HTTP؛ لا تُعد هذه الجولة دليلًا على تنفيذ تلك العمليات من واجهة المتصفح ما لم تُنفذ في جولة UI مستقلة.


## ملاحظة نزاهة الدليل — أتمتة UI في الجولة الأخيرة

تمت محاولة تشغيل جولة متصفح آلية على viewport هاتفية بعد تثبيت جلسة ADMIN. أظهرت المحاولة أن جلسة المتصفح الآلية لا تثبت المصادقة بشكل موثوق، وتوقفت عند استجابة 401 قبل إكمال التدفق. إحدى المحاولات أنشأت فاتورة مؤقتة قبل توقفها؛ تم فحص MongoDB مباشرة، ثم عكس المخزون والرصيد وحذف الفاتورة وحركاتها. لذلك لا تُسجل هذه المحاولة كاختبار UI ناجح، وتظل إجراءات UI الحساسة غير المكتملة معلّمة في `todo.md`.


## تحديث الميزات: حذف أصناف الفاتورة وقائمة To‑Do

أصبح حوار تعديل الفاتورة يعرض كل السطور مستقلًا، ويظهر زر «إزالة الصنف» عندما تحتوي الفاتورة على أكثر من صنف، مع منع ترك الفاتورة بلا أصناف. عند الحفظ، يستقبل الخادم القائمة الجديدة كاملة، ويستخدم فروق الكميات؛ لذلك يظهر الصنف المحذوف كفرق سالب وتُعاد كميته كاملة إلى المخزون، ويُعاد حساب الإجمالي والمتبقي ورصيد العميل، وتُسجل حركة `INVOICE_EDIT`.

أضيف قسم «المهام والملاحظات» إلى التنقل RTL. يدعم إنشاء المهمة بعنوان وملاحظات وأولوية وتاريخ استحقاق، تبديل الحالة بين مفتوحة ومكتملة، التعديل والحذف، مع عزل مهام المستخدم العادي عن مهام الآخرين والسماح للأدمن بالإدارة. تُحفظ المهام في MongoDB، وتُنسخ إلى IndexedDB، وتدخل عمليات الإنشاء والتعديل والحذف المحلي في طابور المزامنة عند عدم الاتصال.

نجح اختبار التكامل الحي للفواتير بعد إضافة حذف السطر، إذ ثبت استرجاع مخزون الصنف المحذوف وإعادة رصيد العميل من 25 إلى 17 في السيناريو الاختباري ثم إعادته إلى الصفر عند الإلغاء. ونجح اختبار CRUD الحي لقائمة المهام في الإنشاء والعرض والتعديل إلى مكتملة ثم الحذف والتنظيف. كما نجحت `pnpm test` العامة بنتيجة 19 اختبارًا، و`pnpm check`، و`pnpm build`. شُغلت اختبارات التكامل الحية منفردة لأن تشغيلها بالتوازي يسبب تنافسًا على اتصال MongoDB، بينما نجح كل اختبار منفردًا.


## Regression بعد تحسين To‑Do offline

أضيف اختبار Dexie يثبت دمج إنشاء المهمة ثم تعديلها أو حذفها قبل الاتصال، بحيث لا تتكون عمليات متعارضة في الطابور. بعد ذلك نجحت المجموعة العامة في `pnpm test` بعدد 20 حالة ناجحة وحالتين متجاوزتين للاختبارات الحية الاختيارية، ونجح `pnpm check` و`pnpm build`. تحذير حجم حزمة Vite موجود كما في السابق ولا يمنع البناء.


## Branding verification — خيرات Foods

تم تحديث عنوان الصفحة واسم PWA وREADME إلى «خيرات Foods». رُفع الشعار إلى تخزين WebDev واستخدم من مسار `/manus-storage/dc9b3e3d-1e5e-4a72-9ba6-eb6da8fadf3a_34c6f403.png` في شاشة الدخول والشريط الجانبي والشريط العلوي، دون إضافة ملف وسائط محلي إلى مستودع المشروع. أظهرت المعاينة المرئية شعار خيرات Foods في شاشة الدخول، ثم أظهرت جلسة ADMIN الشريط العلوي الأخضر مع كلمة «صباح الخير، أهلاً بك» باللون الأبيض واسم النظام والشعار. نجحت `pnpm check` و`pnpm test` (20 حالة ناجحة، حالتان متجاوزتان للاختبارات الحية) و`pnpm build`.
