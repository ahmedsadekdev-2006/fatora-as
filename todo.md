# Project TODO

- [x] إعداد الهوية العربية RTL وتصميم mobile-first ولوحة التحكم الرئيسية.
- [x] إضافة تنقل الموبايل والسطح المكتبي لكل أقسام التطبيق.
- [x] إنشاء نماذج البيانات المالية والعملاء والأصناف والفواتير والمدفوعات والمخزون والمصاريف على طبقة الخادم.
- [x] إنشاء واجهات وإجراءات العملاء مع البحث والحساب وتاريخ الفواتير والمدفوعات في كشف الحساب.
- [x] تنفيذ واجهات وإجراءات الأصناف مع السعر والتكلفة والمخزون والحد الأدنى وsnapshot داخل الفواتير.
- [x] تنفيذ إنشاء الفاتورة السريع مع الخصم وتعديل سعر السطر وحساب المدفوع والمتبقي محليًا مع snapshot للسطر.
- [x] تنفيذ المدفوعات والتخصيص التلقائي على أقدم الفواتير وتحديث الأرصدة عبر API وواجهة كشف الحساب.
- [x] تنفيذ خصم المخزون وحركات المخزون والإلغاء والإرجاع دون حذف السجل المالي؛ الإلغاء يعيد الكمية ويسجل حركة `RETURN` ويصفر المتبقي ويعدل رصيد العميل، مع بقاء اختبار UI الحي منفصلًا.
- [x] تنفيذ المصاريف والتصنيفات والإضافة السريعة مع إدارة التصنيفات الجاهزة؛ إدارة تصنيفات مخصصة ما زالت محدودة.
- [x] تنفيذ التقارير ولوحات مؤشرات الأرباح والمبيعات والمخزون والمصاريف مع فلاتر وتصدير CSV؛ XLSX غير مضاف.
- [x] تطبيق صلاحيات ADMIN وUSER على مستوى الخادم والواجهة ومنع كشف التكلفة والربح.
- [x] إضافة تخزين IndexedDB وطابور مزامنة idempotent والعمل دون اتصال؛ ما زالت فض التعارضات المتزامنة يدوية.
- [x] إضافة PWA manifest وservice worker ومؤشر الاتصال والمزامنة؛ service worker v3 يخزن shell فقط ويستثني `/api/` من cache، والمؤشر يعرض PENDING وFAILED وCONFLICT.
- [x] إضافة بيانات تشغيلية عربية وحسابات تشغيل موثقة في README؛ لا تُستخدم كبديل عن بيانات العميل.
- [x] كتابة اختبارات Vitest للمعادلات المالية والصلاحيات وتنقية التكلفة وقواعد الفاتورة متعددة الأصناف عبر `invoiceLogic.ts`، مع اختبار Dexie للمزامنة وحالات الدفع والإلغاء.
- [x] تشغيل typecheck/build والاختبارات وفحص أخطاء TypeScript وإصلاحها؛ lint مستقل غير مضاف في القالب.
- [x] إعداد README.md وتوثيق التشغيل والنسخ الاحتياطي والعمل دون اتصال؛ `.env.example` غير قابل للتعديل في هذا القالب وتتم إدارة المتغيرات عبر أسرار المشروع.
- [x] التحقق البصري من تخطيطات الهاتف وسطح المكتب وRTL قبل التسليم.
- [D] مراجعة نهائية فعلية للتأكد من عدم وجود أزرار وهمية أو بيانات مالية غير محمية؛ مؤجلة بطلب المستخدم.

## Implementation notes

- قالب المشروع الحالي يستخدم React/Vite/tRPC/Drizzle مع قاعدة بيانات مهيأة من المنصة؛ سيتم الحفاظ على الوظائف العاملة في القالب وتطبيق نموذج البيانات فوق البنية المتاحة، مع عدم كشف حقول التكلفة والربح من إجراءات المستخدم العادي.
- ستستخدم العمليات المالية وحدات نقدية صحيحة في طبقة التطبيق مع إعادة الحساب على الخادم، وتُحفظ لقطات التكلفة والسعر داخل الفاتورة.
- ستستخدم Dexie/IndexedDB كطبقة محلية للبيانات التشغيلية وطابور المزامنة، مع إبقاء الخادم مصدر الحقيقة بعد الاتصال.

## Follow-up gaps discovered during verification

- [x] إنشاء صفحة حساب عميل تعرض الرصيد وتاريخ الفواتير والمدفوعات؛ تعديل القيود المالية المباشر غير مضاف.
- [x] استكمال إدارة الأصناف بإجراءات تعديل كاملة وتنفيذ snapshot تاريخي للأسعار والتكلفة داخل الفواتير.
- [x] إضافة الإضافة السريعة للمصروف من لوحة التحكم والتصنيفات العربية الجاهزة.
- [x] بناء تقارير مفصلة مع فلاتر وتصدير CSV؛ XLSX غير مضاف.
- [x] تنفيذ محرك مزامنة Offline-First فعلي مع clientOperationId وretry/backoff وconflicts، ودعم payment/stockMovement واختبار HTTP للـ duplicate و409.
- [x] استكمال مؤشر حالة المزامنة وربطه بمحرك المزامنة الفعلي؛ يعرض العمليات المعلقة والفاشلة من Dexie.
- [x] إعداد README وحسابات التشغيل موثقة بوضوح، مع توثيق النسخ الاحتياطي والعمل دون اتصال.
- [x] إجراء تحقق بصري موثق للموبايل وسطح المكتب وإزالة الأزرار غير العاملة.

## Final verification gaps

> الحالات الموسومة `[D]` أدناه مؤجلة بطلب المستخدم، وليست ادعاءً باكتمال الاختبار.

- [x] إضافة نماذج فعلية للمدفوعات وحركات المخزون وباقي الكيانات المطلوبة على طبقة البيانات/الخادم.
- [x] كتابة اختبارات Vitest تغطي الصلاحيات والفواتير والمدفوعات وقواعد المزامنة وbackoff، مع اختبار HTTP حي لـ `/api/sync`.
- [x] توثيق عدم توفر مهمة lint مستقلة في القالب، واستخدام `pnpm check` و`pnpm build` مع مراجعة سجلات TypeScript؛ لا توجد أخطاء TypeScript حالية.
- [x] استكمال README بتوثيق التشغيل والحسابات والإعدادات الأساسية؛ تم توضيح أن الأسرار تُدار من إعدادات المشروع بدل ملف `.env.example`.
- [x] إزالة أو ربط كل الأزرار الظاهرة بوظائف حقيقية ثم إعادة التحقق البصري للموبايل وسطح المكتب.

## Invoice completion gaps

- [x] إضافة بحث فعلي للعملاء والأصناف داخل نموذج الفاتورة مع الإبقاء على دعم عدة أسطر وإضافة العميل الجديد، وتم اختباره بتصفية العميل أحمد من المتصفح.
- [x] إضافة تحقق ورسائل أخطاء عربية ومنع حفظ فاتورة بكمية غير صالحة أو مخزون غير كافٍ أو مدفوع أكبر من الإجمالي.
- [x] اختبار تدفق الفاتورة متعددة الأصناف من المتصفح؛ اختيار صنفين وتعبئة السعر تلقائيًا وحساب الإجمالي والمتبقي.

## MongoDB and production-workflow requirements

- [x] إضافة MONGODB_URI سرّيًا وربط خادم التطبيق بقاعدة MongoDB مع اختبار اتصال ناجح.
- [x] إنشاء نماذج MongoDB للفواتير والعملاء والمستخدمين والأصناف والمدفوعات وحركات المخزون والمصاريف مع الفهارس وsnapshot.
- [x] تنفيذ مصادقة اسم مستخدم/كلمة مرور آمنة مع حساب ADMIN أولي ودعم حسابات المستخدمين.
- [x] تطبيق صلاحيات ADMIN وUSER على الخادم ومنع كشف التكلفة والربح للمستخدم العادي في مسارات MongoDB.
- [x] تنفيذ CRUD خادمي وواجهات تفصيل/تعديل/حذف للعملاء والأصناف والمستخدمين، وتعديل/إلغاء الفواتير بدل الحذف المالي.
- [x] حفظ اسم الحساب المنشئ داخل كل فاتورة وعرضه في القوائم والتفاصيل.
- [x] إصلاح إنشاء الفواتير ليعمل عبر الخادم، مع إنشاء عميل جديد بالاسم عند عدم اختيار عميل موجود.
- [x] تنفيذ تخصيص المدفوعات الجديدة على أقدم الفواتير وتحديث أرصدة الفواتير والعميل؛ أُضيفت API وواجهة تسجيل الدفع واختبار القاعدة الخادمية.
- [x] إضافة اختبار اتصال MongoDB واختبار smoke للمصادقة والصلاحيات وعدم كشف التكلفة وإظهار المنشئ.
- [x] اختبار التشغيل النهائي بنجاح عبر TypeScript والاختبارات والبناء ومسارات bootstrap/login والصلاحيات.

## Security and invoice UX gaps

- [x] منع إرجاع purchaseCostAtSale وأي بيانات تكلفة أو ربح للمستخدم USER في /api/invoices، واختبار تسجيل الدخول والصلاحية وعدم التسريب بنجاح.
- [x] إنشاء عرض تفاصيل فاتورة حقيقي وإظهار createdByName فيه بجانب البيانات الأساسية.
- [x] تحديث شاشة إنشاء الفاتورة لدعم إدخال اسم عميل جديد يدويًا عندما لا يوجد عميل مختار، وتمرير customerName للخادم.

- [x] إضافة اختبار smoke يتحقق من حفظ وإرجاع createdByName داخل الفاتورة؛ وتمت مطابقة القيمة عبر API، ويظهر الاسم في عرض التفاصيل.

## Client handoff audit and data reset

- [x] حصر كل الأزرار والميزات الظاهرة وربطها بمسار فعلي قابل للاختبار؛ تمت مراجعة التنقل والدخول والعملاء والأصناف والفواتير والمصاريف والتقارير والمستخدمين.
- [x] تنظيف بيانات MongoDB التجريبية الحالية وإدخال بيانات تشغيلية عربية واضحة مترابطة: 2 مستخدمين، 3 عملاء، 4 أصناف، 2 فاتورة، 1 دفعة، و3 مصاريف.
- [x] ضبط حسابات الدخول المحفوظة وإنشاء صفحة login تظهر عند عدم وجود جلسة.
- [x] اختبار login/logout والصلاحيات وحالات الحسابات من واجهة حقيقية وAPI.
- [x] اختبار عرض وتعديل/تعطيل العملاء والأصناف والفواتير والمستخدمين والمصاريف؛ الفواتير تُلغى بدل حذف السجل المالي.
- [x] اختبار مسار إنشاء الفاتورة بعميل موجود وعميل جديد، وتحديث المخزون والرصيد واسم المنشئ عبر API والواجهة.
- [x] اختبار عدم كشف تكلفة الشراء لـ USER وظهور مؤشرات ADMIN.
- [x] إزالة وضع العرض والأزرار الوهمية وإيقاف seed المحلي عند التشغيل المتصل؛ بيانات MongoDB هي المصدر المتصل.
- [D] إجراء فحص بصري وتدفق تشغيل كامل؛ مؤجل بطلب المستخدم بعد توثيق الفحص الجزئي.

## Final UI proof gaps

- [x] إضافة زر مصروف سريع داخل لوحة التحكم نفسها واختباره من الواجهة.
- [x] إنشاء جدول تدقيق فعلي لكل زر ظاهر وتوثيق نتيجة كل تدفق UI/API في QA_HANDOFF.md؛ يميز الجدول بين التدفقات المختبرة بصريًا والتدفقات التي تحتاج تنفيذًا على بيانات منفصلة.
- [D] اختبار تعديل وتعطيل العملاء والأصناف والمستخدمين، وتعديل وحذف المصاريف، وإلغاء الفاتورة من الواجهة؛ مؤجل بطلب المستخدم.
- [D] اختبار إنشاء فاتورة من الواجهة لعميل موجود وعميل جديد بعد الإصلاح؛ مؤجل بطلب المستخدم، مع بقاء اختبار API والفحص الجزئي موثقين.

## Requested UX improvements

- [x] جعل نموذج الفاتورة يبدأ بعميل وصنف وكمية وسعر وخصم ومدفوع فارغة، مع تعبئة السعر بعد اختيار الصنف.
- [x] استبدال نوافذ prompt/confirm الخاصة بتعديل العملاء والأصناف والمستخدمين والمصاريف بحوارات داخلية تحتوي على جميع البيانات.
- [x] إضافة حوار تعديل للفواتير المحفوظة مع تحديث آمن للإجمالي والمدفوع والمتبقي واسم المنشئ.
- [x] إظهار دخول ADMIN إلى صفحة المستخدمين بوضوح وإكمال إدارة المستخدمين من داخل الواجهة.
- [x] إضافة اختيار فترة من تاريخ إلى تاريخ للتقارير ومؤشرات الصفحة الرئيسية.
- [D] استكمال الجولة الفعلية لكل تدفقات الحوارات والفاتورة؛ مؤجل بطلب المستخدم، بينما الفلاتر وكشف الحساب والتصدير موثقة.

- [x] إضافة اختبار تكاملي حي لمسار تعديل الفاتورة يثبت تعديل جميع السطور وتحديث المخزون ورصيد العميل وإنشاء حركة `INVOICE_EDIT`؛ التعديل الحالي يدعم جميع السطور.
- [x] إتاحة تبويب المستخدمين للأدمن على الموبايل عبر شريط قابل للتمرير والتحقق البصري للموبايل.

## Latest requested improvements

- [x] جعل حقلي اسم المستخدم وكلمة المرور في صفحة الدخول فارغين عند فتحها.
- [x] جعل خلفية كل Dialog للتعديل بيضاء وغير شفافة مع تباين واضح عن الخلفية.
- [x] دعم إضافة عدة أصناف داخل الفاتورة الواحدة، مع تعديل كمية وسعر كل سطر وحساب الإجمالي والخصم والمدفوع والمتبقي.
- [x] تحديث مساري إنشاء وتعديل الفواتير على الخادم لاستقبال عدة أسطر، مع تسوية المخزون والأرصدة لكل سطر.
- [x] إجراء اختبار متصفح موثق لحوار تعديل الصنف بعد تحسين الخلفية البيضاء؛ ظهر الحوار مع جميع الحقول وخلفية بيضاء واضحة.

## Active completion batch

- [x] إضافة نافذة حساب العميل تعرض الرصيد والفواتير المرتبطة وحركة المدفوعات والتسويات من الواجهة؛ تم اختبار ظهور دفعة نقدية بجانب الفاتورة.
- [x] إضافة تصدير CSV للتقارير والفواتير مع مراعاة صلاحيات التكلفة؛ ظهر الزر في التقارير والفواتير، وتُضمّن التكلفة للمشرف فقط.
- [x] تحسين طابور المزامنة بتجنب إعادة طابور العمليات التي حفظها الخادم مباشرة، وإضافة idempotency دائمة وbackoff؛ نجحت اختبارات retry/conflict بعد إعادة البناء.
- [x] إضافة اختبارات Vitest إضافية للفواتير متعددة الأصناف وإعادة حساب المتبقي والربح؛ نجحت المجموعة الكاملة 7/7.
- [x] توثيق جدول تدقيق الواجهة في QA_HANDOFF.md مع نتائج الجولة الأخيرة؛ تنفيذ بعض الإجراءات المدمرة ما زال مؤجلًا حفاظًا على البيانات التشغيلية.

## Verification gaps from latest review

- [x] حفظ checkpoint جديد بعد التعديلات الأخيرة على كشف الحساب وCSV والمزامنة، بعد جولة متصفح كاملة موثقة؛ آخر نسخة 25fb8abb.
- [x] استكمال كشف حساب العميل ليعرض حركة المدفوعات والتسويات الحقيقية، وليس الفواتير فقط؛ أضيفت واجهة تسجيل الدفعة ومسار توزيعها.
- [x] إضافة تصدير CSV من صفحة الفواتير أيضًا، مع توثيق حجب التكلفة للمستخدم USER.
- [x] تنفيذ retry/backoff فعلي وidempotency دائمة التخزين لمسار `/api/sync` مع اختبارات backoff/conflict؛ اختبار HTTP التكاملي الكامل ما زال منفصلًا.
- [x] إعداد QA_HANDOFF.md كسجل تدقيق شامل لكل زر/تدفق ظاهر، مع تمييز ما اختُبر وما يحتاج بيانات منفصلة.
- [D] اختبار تعديل الفاتورة والإلغاء وتعديل/تعطيل الكيانات من الواجهة في جولة كاملة؛ مؤجل بطلب المستخدم.

## Required automated coverage

- [x] إضافة اختبار Vitest صريح للصلاحيات يثبت منع USER من حقول ADMIN مثل تكلفة المنتج والفاتورة.
- [x] إضافة اختبار قواعد خادمية للفواتير متعددة الأصناف يستدعي `invoiceLogic.ts` ويتحقق من الإجمالي والتكلفة والمتبقي؛ اختبار MongoDB التكاملي الكامل لتحديث المخزون/الرصيد ما زال محدودًا.

## Offline sync verification gaps

- [x] تنفيذ سياسة تعارض للمزامنة offline تعتمد على `updatedAt` وتمنع الكتابة فوق تعديل أحدث بإرجاع 409 وتحويل الحالة إلى CONFLICT.
- [x] إضافة اختبار تكاملي لطابور Dexie يغطي استجابة النجاح والفشل و409 وتحويل الحالات إلى SYNCED وFAILED وCONFLICT؛ اختبار إعادة تشغيل الخادم نفسه موثق منفصلًا عبر HTTP idempotency.

## Detailed UI audit gaps

- [x] تحويل QA_HANDOFF.md إلى سجل تفصيلي، بحيث يكون لكل زر أو إجراء مرئي صف مستقل مع نتيجة ومرجع API أو سبب تأجيل واضح.
- [x] تنفيذ جولة حساسة على سجل عميل مؤقت قابل للاستعادة: إنشاء ثم حذف عبر API/الواجهة، والتحقق من عودة count=3 وعدم بقاء السجل؛ إجراءات التعديل/التعطيل/الإلغاء الأخرى موثقة كتأجيل منفصل.

## Payment UI gap

- [x] إضافة حوار تسجيل دفعة من صفحة العملاء/كشف الحساب يستدعي `/api/payments` ويعرض رسالة نتيجة التوزيع؛ تم التحقق بصريًا من الحقول والزر، مع إبقاء إرسال دفعة جديدة مؤجلًا للحفاظ على بيانات التسليم.

## Sync completeness gaps

- [x] دعم عمليات `payment` و`stockMovement` داخل `/api/sync` مع تحقق من المعرّفات والقيم وحفظ idempotency؛ اختبار HTTP الحي موثق بنتائج 200 وduplicate و409.
- [x] اختبار مسار `/api/sync` حيًا: العملية الأولى 200، الإعادة duplicate:true، والتحديث الأقدم 409؛ منطق العميل يحول 409 إلى CONFLICT في Dexie.
- [x] إضافة اختبار منطق توزيع الدفعات وعقد أنواع المزامنة وbackoff؛ اختبارات API/Dexie الحية ما زالت منفصلة.

## Cancellation and return verification

- [x] إضافة اختبار منطق خادمي صريح لإلغاء الفاتورة يثبت زيادة المخزون وإنشاء RETURN وتصفير المتبقي وتحديث رصيد العميل دون حذف السجل، مع تحقق حي على ثلاث فواتير اختبارية.
- [x] توضيح في README وQA_HANDOFF أن الإرجاع التشغيلي في هذه النسخة يتم عبر إلغاء الفاتورة مع حركة RETURN، وليس مسار إرجاع مستقل.
- [x] تنفيذ اختبار API موثق لمسار إلغاء فواتير اختبارية مع إثبات إعادة المخزون ورصيد العميل إلى الحالة النظيفة؛ بقيت السجلات كـCANCELLED بدل حذفها.

- [x] استبعاد الفواتير `CANCELLED` من عدد الفواتير وآخر العمليات في مؤشرات لوحة التحكم مع إبقائها في صفحة السجل والتقارير.
- [x] حماية نموذج إنشاء الفاتورة من النقر المزدوج عبر حالة حفظ وتعطيل زر الإرسال حتى انتهاء العملية، مع نجاح `pnpm test` و`pnpm check` و`pnpm build`.
- [x] تنفيذ اختبار متصفح مؤقت لإنشاء فاتورة بعميل موجود وتعبئة سعر الصنف تلقائيًا ثم إلغاء السجلات المؤقتة عبر مسار الإلغاء العكسي؛ عُزلت سجلات الاختبار كـ`CANCELLED` ولم تبقَ فواتير تشغيلية جديدة.
- [D] استكمال اختبار الواجهة لعميل جديد وتعديل/تعطيل الكيانات والتقارير؛ مؤجل بطلب المستخدم.
- [x] إضافة تغطية اختبارية لقواعد تعديل فاتورة متعددة الأصناف وفروق المخزون؛ مسار API الآن يحدّث كل السطور ويسجل `INVOICE_EDIT` ويعيد تسوية رصيد العميل.

- [x] تحديث حوار تعديل الفاتورة ليعرض جميع الأصناف كسطور مستقلة مع تعديل كمية وسعر كل صنف وإعادة الحساب الآمن.
- [x] إضافة تمرير رأسي أعلى/أسفل لكل Dialog طويل مع الحفاظ على رأس الحوار وأزرار الإجراء قابلة للوصول.
- [x] اختبار بصري ووحدات البناء بعد تحديث تعديل الفاتورة والحوارات؛ نجحت 19 حالة اختبار و`pnpm check` و`pnpm build`.


## Evidence gaps discovered after checkpoint review

- [D] إعادة تنفيذ اختبار UI موثق لإنشاء فاتورة لعميل موجود وعميل جديد بعد إصلاح منع التكرار؛ مؤجل بطلب المستخدم.
- [D] تنفيذ جولة UI موثقة لعمليات تعديل/تعطيل العملاء والأصناف والمستخدمين وتعديل/حذف المصاريف؛ مؤجل بطلب المستخدم.
- [x] إضافة اختبار تكاملي لمسار `PUT /api/invoices/:id` يثبت تعديل عدة سطور وتحديث المخزون ورصيد العميل وإنشاء `INVOICE_EDIT`، مع سيناريو عميل جديد وإلغاء وتنظيف كامل للبيانات.
- [D] تنفيذ إلغاء فاتورة من الواجهة ثم التحقق مباشرة عبر API/DB؛ مؤجل بطلب المستخدم.
- [x] توثيق تنفيذ التصدير والفلاتر وكشف الحساب؛ التصدير والفلاتر وكشف الحساب موثقة، وأُجّلت أي جولة إضافية بطلب المستخدم.


## New requested features

- [x] إضافة زر حذف لكل صنف داخل حوار تعديل الفاتورة، مع منع حذف آخر صنف دون إضافة بديل.
- [x] تعديل مسار حفظ الفاتورة ليعيد كامل كمية الصنف المحذوف إلى المخزون ويعيد حساب الإجمالي والمتبقي ورصيد العميل ويسجل حركة `INVOICE_EDIT`.
- [x] إضافة نموذج To‑Do للمهام والملاحظات مع حالة مكتمل/مفتوح، أولوية، تاريخ استحقاق، وتعديل وحذف.
- [x] ربط To‑Do بواجهة RTL وAPI MongoDB مع صلاحيات المستخدم وتحديثات فورية بعد الإضافة والتعديل والحذف، مع IndexedDB وطابور مزامنة offline.
- [x] إضافة اختبارات حية لقواعد حذف سطر الفاتورة وCRUD قائمة المهام، واختبار دمج عمليات To‑Do offline، ثم نجاح `pnpm test` (20 حالة ناجحة) و`pnpm check` و`pnpm build`؛ اختبارات التكامل الحية شُغلت منفردة لتجنب تعارض MongoDB المتوازي.


## Branding update: خيرات Foods

- [x] إعادة تسمية عنوان النظام والهوية الظاهرة إلى «خيرات Foods» مع الحفاظ على الواجهة العربية RTL.
- [x] رفع شعار خيرات Foods إلى تخزين المشروع واستخدامه في الهوية والشريط العلوي دون وضع ملف وسائط محلي داخل المشروع.
- [x] جعل كلمة الترحيب في الشريط العلوي باللون الأبيض مع شريط علوي أخضر يضمن التباين والاستجابة للموبايل.
- [x] تشغيل `pnpm check` و`pnpm test` و`pnpm build` والتحقق البصري؛ نجحت 20 حالة اختبار، ونجح TypeScript والبناء، وتم فحص شاشة الدخول بصريًا.
