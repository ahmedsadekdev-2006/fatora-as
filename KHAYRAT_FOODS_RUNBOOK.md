# خيرات Foods — دليل التشغيل والتسليم

## المتطلبات

يحتاج المشروع إلى Node.js 22 أو أحدث، وpnpm، واتصال MongoDB صالح. يعمل التطبيق بواجهة React/Vite وخادم Express، ويدعم RTL والعمل المحلي عبر IndexedDB والمزامنة عند عودة الاتصال.

## التشغيل المحلي

من داخل مجلد المشروع نفّذ:

```bash
pnpm install
```

اضبط المتغير السري `MONGODB_URI` في بيئة التشغيل المحلية. لا تضع كلمة مرور MongoDB داخل ملفات Git أو داخل هذا الدليل. في بيئة Manus تُدار الأسرار من إعدادات المشروع، أما على جهاز محلي فيمكن استخدام متغير بيئة مؤقت قبل تشغيل الخادم.

بعد ضبط الاتصال شغّل فحوص المشروع:

```bash
pnpm check
pnpm test
pnpm build
```

لتشغيل نسخة التطوير:

```bash
pnpm dev
```

ثم افتح الرابط الذي يظهر في الطرفية، وغالبًا يكون `http://localhost:3000`.

## الحساب الأولي

الحساب الإداري الأولي هو:

```text
اسم المستخدم: admin
كلمة المرور: Admin123!
```

يُنصح بتغيير كلمة المرور أو إنشاء حساب إداري جديد من صفحة المستخدمين قبل التسليم الفعلي للعميل.

## هوية خيرات Foods

تم تحديث اسم الصفحة واسم تطبيق PWA والواجهة إلى «خيرات Foods». الشعار مستخدم في شاشة الدخول والشريط الجانبي والشريط العلوي، ومخزن عبر WebDev Storage. عند تشغيل المشروع خارج بيئة Manus يجب إبقاء مسار الشعار المتصل متاحًا أو استبداله بملف شعار مستضاف في بيئة العميل.

## البيانات والعمل دون اتصال

قاعدة MongoDB هي مصدر الحقيقة عند الاتصال. يحتفظ التطبيق بالعمليات المحلية في IndexedDB، ثم يضع العمليات المؤجلة في طابور مزامنة idempotent عند انقطاع الاتصال. بعد عودة الاتصال تُرسل العمليات تلقائيًا، وتظهر حالات الانتظار أو الفشل أو التعارض داخل مؤشر المزامنة.

## النسخ الاحتياطي

يجب أخذ نسخة احتياطية من MongoDB قبل تشغيل نسخة العميل أو تنفيذ عمليات تنظيف واسعة. استخدم أدوات MongoDB الرسمية أو خدمة النسخ الاحتياطي المرتبطة بمزوّد قاعدة البيانات، ولا تعتمد على IndexedDB كنسخة احتياطية دائمة.

## ملاحظات التسليم

تتضمن النسخة الحالية الفواتير متعددة الأصناف، تعديل كل صنف وحذفه مع إعادة مخزونه عند الحفظ، العملاء والأصناف والمصاريف والمستخدمين، كشف الحساب والمدفوعات، التقارير وفلاتر التاريخ وتصدير CSV، قائمة To‑Do، وصلاحيات ADMIN وUSER. توجد جولات UI إضافية قديمة موثقة في `QA_HANDOFF.md` كفجوات مؤجلة بطلب المستخدم السابق، ولا ينبغي اعتبارها اختبارات إنتاجية بديلة عن اختبار العميل النهائي.
